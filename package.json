{
  "name": "figma-editable-pptx-export",
  "version": "0.1.2",
  "private": true,
  "description": "Figma plugin that exports selected frames to an editable PowerPoint presentation.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
